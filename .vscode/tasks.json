{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "shell",
      "label": "Build Sandbox Project",
      "command": "${workspaceFolder}/scripts/build.bat",
      "args": [],
      "options": {
        "cwd": "${fileDirname}"
      },
      "problemMatcher": ["$gcc"],
      "group": "build"
    }
  ]
}
